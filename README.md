# Expense-Tracker
You can required to build a simple personal finance management tool.
The project should allow the user to:
1) Add all expenses with details lick date, category, description, and amount.
2) View all recorded expenses in a clean format.
3) Calculate total spending so far.
4) Exit the program gracefully when the user chooses to.

